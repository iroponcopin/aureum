pluginManagement {
	repositories {
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenCentral()
		gradlePluginPortal()
	}
}

// mod id と一致させる(mods-src の各モジュールと同じ不変条件)。
rootProject.name = 'aureum'
