accessWidener v2 official
# ブロック状態キャッシュ共有化(dedupeBlockStateCaches)が Cache 型と
# その private フィールドに触るために必要。挙動は一切変えない — 可視性だけ。
accessible class net/minecraft/world/level/block/state/BlockBehaviour$BlockStateBase$Cache
accessible field net/minecraft/world/level/block/state/BlockBehaviour$BlockStateBase$Cache faceSturdy [Z
